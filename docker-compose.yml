version: '3.8'
services:
  tracker:
    build: .
    ports:
      - "8765:8765"
    volumes:
      - tracker_data:/app/data
    environment:
      - SECRET_KEY=change-this-in-production
      - DATABASE_PATH=/app/data/tracker.db
volumes:
  tracker_data:
