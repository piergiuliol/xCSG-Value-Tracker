version: '3.8'
services:
  tracker:
    build: .
    ports:
      - "8765:8765"
    volumes:
      - tracker_data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/tracker.db
    restart: unless-stopped
volumes:
  tracker_data:
